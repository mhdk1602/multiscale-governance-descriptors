[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "governance-descriptors"
version = "0.1.0"
requires-python = ">=3.9"
license = "MIT"
authors = [
    { name = "Dineshkumar Malempati Hari", email = "mhdk.dinesh@gmail.com" },
]

[tool.setuptools.packages.find]
where = ["src"]
